class groupStore {
    map = new Map()
    init(platform,list) {
        switch (platform) {
            case 'qq': {

            }
            case 'qq-bot':
            case 'qq-group-bot':{

            }
        }

    }

export default new groupStore()